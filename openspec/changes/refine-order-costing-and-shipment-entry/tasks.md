## 1. 成本模型与回归测试

- [x] 1.1 在 `src/main/domain/costing.test.ts` 先新增捏毛装袋、缝边成本及完整总成本的失败断言；在 `src/main/services/*` 测试中新增订单预计/实际成本、单任务排班和默认兼职时薪的失败用例。
- [x] 1.2 修改 SQLite 迁移、共享合同、商品快照与成本领域计算，增加捏毛装袋和缝边成本，并兼容旧订单快照缺失字段。
- [x] 1.3 修改订单创建/更新与成本预览：缝边数量自动等于商品数量，预计成本包含全部成本项和默认时薪人工。
- [x] 1.4 实现可复用订单成本明细和总额计算：实际人工仅汇总已完成单任务排班的基础分钟加额外预留分钟，并以排班日期人员时薪计算；同步 `orders.actual_cost_cents`。

## 2. 排班与人员业务规则

- [x] 2.1 修改排班预览、保存、编辑和状态完成逻辑，前后端强制一次排班恰好一个订单商品任务，并在完成时刷新所属订单实际成本。
- [x] 2.2 修改新建兼职人员表单，使时薪默认带入当前系统默认时薪且仍允许编辑；补充渲染契约测试。

## 3. 订单成本与发货界面

- [x] 3.1 先在 `src/renderer/pages/app-components.test.ts` 写入订单成本仅通过弹窗查看、成本项展示、单任务排班和新增发货弹窗/历史只读的失败契约。
- [x] 3.2 修改商品录入/编辑与成本预览界面，增加捏毛装袋成本和缝边成本；修改订单商品界面，使缝边数量不再可编辑且自动同步数量。
- [x] 3.3 修改订单详情：移除主页面成本数值，增加成本详情弹窗及按商品、按成本项的预计/实际展示。
- [x] 3.4 修改发货区域：仅展示汇总、历史和新增发货按钮；用新增发货弹窗录入，移除历史发货编辑入口。
- [x] 3.5 调整所需 CSS 并运行针对性渲染组件测试。

## 4. 验证与交付

- [x] 4.1 依次运行 `npm test`、`npm run typecheck`、`npm run lint`、`npm run build`，修复阻塞问题并记录新鲜验证结果。
- [x] 4.2 运行 `openspec validate refine-order-costing-and-shipment-entry --strict`，更新任务清单和方案文档的实施/验证记录。
- [x] 4.3 使用路径限定暂存检查变更并提交中文 Conventional Commit；不推送远端。
