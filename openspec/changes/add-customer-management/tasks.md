## 1. 共享模型与客户管理契约

- [x] 1.1 在 `src/shared/contracts.ts` 拆分客户主档输入、订单客户关联/快照输入，并补充客户管理列表项、详情、聚合和删除结果的 contract；保持订单下拉所需轻量客户资料兼容。
- [x] 1.2 在 `src/main/repositories/studio-repository.ts` 为客户主档实现可检索的管理列表、带订单汇总和历史的详情、更新与受关联保护的删除，并在事务中处理删除竞争。
- [x] 1.3 在 `src/main/services/studio-service.ts` 增加客户管理新增、查询、编辑和删除的输入校验与稳定业务错误映射。
- [x] 1.4 新增或扩展服务层测试，先覆盖客户新建、编辑、名称/联系方式/地址检索、无进行中订单仍可查询、详情只统计自身订单、收款/退款后未结清金额更新，以及有/无关联订单时的删除结果。

## 2. 订单客户关联与快照隔离

- [x] 2.1 先扩展 `src/main/services/order-management.test.ts`：创建或编辑订单缺少客户标识必须失败；选择已有客户后修改订单联系方式或地址不得更新客户主档；编辑客户主档不得改写历史订单快照。
- [x] 2.2 调整 `src/main/services/studio-service.ts` 的订单创建和更新校验，使订单客户标识必填且必须引用存在的客户，移除订单路径中接受临时客户的输入语义。
- [x] 2.3 调整 `src/main/repositories/studio-repository.ts` 的订单创建与更新：删除缺少客户标识时自动插入客户的分支，仅读取已存在客户并保存订单级快照；保持历史订单快照读取兼容。
- [x] 2.4 运行并修正客户与订单服务层测试，使订单改绑客户、支付/退款后客户汇总和删除保护均保持正确。

## 3. IPC 与预加载接口

- [x] 3.1 在 `src/main/ipc/register-ipc.ts` 注册客户管理的列表、详情、新建、更新和删除通道，并将服务层业务错误传递为界面可提示的错误。
- [x] 3.2 在 `src/preload/index.ts` 和共享窗口类型中暴露客户管理接口，同时保留订单编辑器使用的既有客户列表接口。
- [x] 3.3 补充 IPC/preload 相关测试或类型断言，验证新增接口参数与返回值符合共享 contract。

## 4. 客户管理与订单编辑界面

- [x] 4.1 新建独立客户管理页面组件，提供客户列表、名称/联系方式/默认收货地址检索、新建与编辑表单、详情汇总和关联订单历史展示。
- [x] 4.2 在 `src/renderer/pages/app.tsx` 增加客户管理导航入口和页面切换，仅承担页面装配、刷新和返回订单等顶层协调；与现有并行修改先完成合并边界确认。
- [x] 4.3 调整 `OrderEditorDialog`：移除“新建客户/手填临时客户”分支，要求选择已有客户；选择后预填客户资料，允许编辑当前订单快照，并在未选择时阻止保存和引导至客户管理。
- [x] 4.4 补充或调整渲染层组件测试，覆盖客户管理入口、订单未选客户的保存阻止、选择客户的资料预填以及订单快照编辑不回写主档的交互。

## 5. 回归验证与交付检查

- [x] 5.1 在保留历史 SQLite 数据的测试夹具上回归订单详情读取，确认旧快照不被客户管理改写且本次不触发数据库表重建。
- [x] 5.2 运行 `npm run typecheck`、`npm run lint`、`npm test` 和 `npm run build`，修复由本变更引入的问题。
- [ ] 5.3 按客户 CRUD、删除保护、订单必选已有客户、快照隔离、关联订单范围及付款/退款汇总刷新执行桌面端手工验收。
- [x] 5.4 运行 `openspec validate add-customer-management --strict`，在实现完成后更新本任务清单和相关方案文档的实施记录。
