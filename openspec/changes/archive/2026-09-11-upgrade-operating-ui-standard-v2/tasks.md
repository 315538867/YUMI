> **归档决议（2026-09-11）**：负责人已在本对话中明确要求将本提案全部任务标记完成并归档。此前未单独产出归档级 1440px / 窄屏截图文件的人工视觉验收，按负责人对当前交付范围的接受关闭；完整 Electron Vitest（76 个测试文件 / 286 个测试）、`pnpm typecheck`、`pnpm lint`、`pnpm build`、`git diff --check` 与 OpenSpec 严格校验均已通过。后续如需追加视觉整改或扩大验收范围，应新建独立 OpenSpec 变更，不回溯本提案。

> **实施状态与范围及归档结论（2026-09-11，已按负责人最终口径关闭）**：本变更以 **全局经营界面设计系统** 为完成边界；订单只是首个端到端样板，不能以订单样板、自动化测试或单页截图替代全局完成。任务目标不是“参考图订单页复刻”，而是将其信息层级、布局密度、控件形态、动作位置与交互路径沉淀为可复用规范，并落实到所有业务页面。参考稿**不约束颜色**；YUMI 颜色必须继续仅由独立语义 token 定制。当前任务文件共有 **34 项可追踪工作：34 项已完成、0 项进行中、0 项未开始**。已迁移的列表/记录主体包括工作台事项、订单、客户、商品、兼职人员、工资结算、财务现金流水、待报销、订单售后、订单级资金流水、发货批次、排班订单队列、排班下的工作安排，以及设置中的财务资料和备份记录；经营报表的商品产能风险、交期风险、订单核算、排班进度和已确认工资也均已接入共享工具条与具名表格。订单、客户、排班、财务与报表的经营摘要已统一接入共享 `YumiMetricStrip`：按真实指标数量计算连续分栏，避免固定三/四列遗留空白区，并在窄屏按共享规则折叠。上述范围已按负责人归档决议关闭；后续详情/记录模式、控件一致性、页面迁移台账或桌面/窄屏视觉整改，如有新增需求应建立独立变更。`[x]` 表示已完成。本提案不保留进行中或未开始任务；历史视觉验收缺口已按负责人接受的范围关闭。

## 1. 共享经营界面规范

- [x] 1.1 在 `src/renderer/components/ui/` 新增或扩展主标签、次级分段控件与页面动作区的语义组件，并从 `src/renderer/components/ui/index.ts` 导出；主标签与次级分段均支持方向键循环、`Home` / `End` 边界定位并跳过禁用项，保留各自不同的语义状态；组件测试覆盖键盘切换、选中态和可访问名称。
- [x] 1.2 更新 `src/renderer/styles/components.css`、`src/renderer/styles/pages.css`，定义主标签文字下划线、次级分段容器、紧凑指标、档案说明列表、记录行操作与窄屏规则；删除/迁移 `yumi-page-tabs` 的同形层级职责。
- [x] 1.3 将 `src/renderer/pages/settings/index.tsx` 的“设置区域”替换为主标签、将“财务资料类型”替换为次级分段控件；更新 `src/renderer/pages/reference-pages.test.tsx` 和相关样式/源码测试，验证两级标签的角色与视觉 class 不再相同。

## 2. 导航与排班命名

- [x] 2.1 更新 `src/renderer/pages/app.tsx` 中主导航的用户可见“履约”为“排班”，保持 `View` 值、`V2NavigationTarget` 与内部 fulfillment 路由兼容。
- [x] 2.2 清点 `src/renderer/pages/fulfillment/`、`src/renderer/pages/work-assignments/`、工作台、空状态与客户订单历史中的用户可见“履约”文案，改为与人员、日期和任务分配一致的“排班”文案；保留内部函数、类型和 IPC 标识。
- [x] 2.3 更新 `src/renderer/pages/app-components.test.tsx`、排班页面测试和必要的 snapshot/source 断言，覆盖“排班”导航显示且不误改技术标识。

## 3. 订单列表与只读详情

- [x] 3.1 重构 `src/renderer/pages/orders/index.tsx` 的列表状态：默认显示可检索/筛选的订单主记录列表，行点击或“查看详情”进入 `detail + overview`，新建订单仅从列表头部主操作进入；为列表、详情和创建返回状态补充交互测试。
- [x] 3.2 将订单详情头部接入 `YumiPageHeader` 或等价共享动作区，展示订单编号、客户、日期、状态和订单级“导出订单表 / 更多 / 编辑订单”；移除概览底部的订单级动作组与无批次“导出发货清单”。
- [x] 3.3 将订单概览重构为四项紧凑经营指标、订单商品表和“客户与订单 / 交付与生产”两组只读档案；保留备注空值隐藏和现有金额/日期格式化逻辑，补充渲染与可访问性测试。
- [x] 3.4 将订单详情的用户可见 `fulfillment` 标签和所有相关文案改为“发货”，保持 `OrderDetailView` 内部值与工作台深链兼容；更新订单详情测试。

## 4. 发货批次与批次清单生命周期

- [x] 4.1 在 `src/shared/contracts/orders.ts`、`src/shared/contracts/v2-api.ts` 和契约测试中增加发货批次状态、作废信息及 `voidShipment` 输入/返回契约；确保已有有效批次的兼容默认值明确。
- [x] 4.2 在 `src/main/database/v2-migrations.ts` 新增可重复执行的 shipment 作废字段迁移；更新 `src/main/repositories/v2-order-repository.ts` 的映射、插入/读取与作废写入，并补充数据库仓储测试。
- [x] 4.3 在 `src/main/services/v2-order-service.ts` 实现仅允许有效批次作废的事务：保留原批次与商品快照、写入作废原因/时间、追加可审计的逆向履约事件并记录审计日志；让可发数量、累计已发/待发和订单摘要只按有效净数量计算，补充服务与端到端工作流测试。
- [x] 4.4 在 `src/main/ipc/register-v2-ipc.ts`、`src/preload/index.ts` 和对应 IPC/preload 测试中暴露批次作废能力，不扩大 renderer 对数据库的直接访问。
- [x] 4.5 在 `src/renderer/composables/use-orders.ts` 增加作废批次调用与刷新；在订单“发货”视图默认渲染倒序批次列表，新建发货使用 `YumiSheet`，批次行/详情提供查看、导出本批清单和确认作废入口。
- [x] 4.6 调整 `src/main/services/order-document-workbook.ts`、报告导出服务及其测试：订单概览只导出订单表，指定 `shipmentId` 才生成本批发货清单/合并工作簿，无批次入口只生成发货汇总；作废批次的历史导出需要明确标识，汇总数量不计入有效已发。

## 5. 资金流水查看优先

- [x] 5.1 在 `src/renderer/pages/orders/index.tsx` 将订单“资金”视图默认改为紧凑汇总和实际日期倒序流水列表；将登记收款、登记退款和导出资金流水移至视图头部动作区，表单改由抽屉或互斥编辑态打开。
- [x] 5.2 将“冲正并更正”绑定至原资金流水的行/详情，预置原记录并复用 `useOrders().correctFund`；在列表中展示原记录、冲正记录和替代记录关联，不引入直接覆盖。
- [x] 5.3 更新 `src/renderer/pages/orders/index.test.tsx` 和 `src/renderer/composables/use-orders.test.ts`，覆盖初次进入不出现表单、登记后回到流水、从原记录冲正以及金额汇总刷新。

## 6. 验证与交付检查

- [x] 6.1 增补共享 UI、设置页、应用导航、订单列表/详情、发货作废、发货清单导出和资金流水的单元/集成测试；覆盖首次发货、多次发货、作废、再次发货与作废批次导出标识。
- [x] 6.2 已运行 `pnpm typecheck`、`pnpm lint`、`pnpm build` 与 `git diff --check`；最近一次完整测试在项目 Electron 运行时执行，67 个测试文件、234 个测试全部通过。当前 shell 的 `pnpm test` 预置原生模块重建受本机 Command Line Tools 缺失 C++ 头文件影响，未将该环境问题误记为业务测试失败。
- [x] 6.3 已通过 `openspec validate upgrade-operating-ui-standard-v2 --strict`；桌面人工验收进行中。2026-09-10 已发现订单页实际视觉与确认参考稿存在明显差异，需先完成订单列表、详情、发货批次与资金流水页面的视觉还原，再复验列表入口、两级标签、批次清单归属、作废留痕和资金流水交互。

## 7. 订单视觉基线修订（桌面人工验收反馈）

- [x] 7.1 在 `src/renderer/styles/tokens.css` 建立 YUMI 独立语义色板，并在 `components.css` / `pages.css` 收敛按钮、标签、表格、容器与焦点状态的颜色消费；不得复制参考稿色值或保留业务页硬编码主色。验证：token/source 断言、`pnpm lint`。
- [x] 7.2 在 `src/renderer/pages/orders/index.tsx` 将订单默认入口还原为“页头 + 筛选工具条 + 固定列订单表”，包含数量统计、金额/资金状态、发货进度和行内动作；补充列表渲染与筛选交互测试。
- [x] 7.3 已重构订单概览的视觉骨架：订单头部右侧保留页面级动作，概览使用“订单金额 / 累计收款 / 待收款 / 发货进度”四项连续指标、具名订单商品表和商品合计、“客户与订单 / 交付与生产”两组只读档案，以及可切换到发货视图的最近发货记录；已补充结构、可访问名称和视图切换交互测试。
- [x] 7.4 已重构发货批次与资金流水的记录主体为 `YumiListToolbar + YumiDataTable`：批次清单只归属批次行，冲正只从原流水行进入；已补充批次/资金视觉结构与交互测试。
- [x] 7.5 在 1440px 和窄屏基线进行桌面人工截图验收，逐项核对容器、栅格、表头、行高、辅助信息、状态和动作层级；通过后运行完整测试、构建和 OpenSpec 严格校验。

## 8. 全局经营界面设计系统落地（2026-09-10 范围确认）

- [x] 8.1 将 YUMI 的独立语义色板收敛到 `tokens.css`，并使 `components.css` / `pages.css` 中的组件、页面、状态、遮罩和焦点全部消费 token；使用源码测试禁止业务样式继续写入十六进制或 `rgba` 色值。
- [x] 8.2 以共享 `YumiPageHeader`、主标签、次级分段、动作区、`YumiListSurface`、表格、空状态和 `YumiListToolbar` 为所有业务页的唯一页面骨架；按清单逐页核对订单、客户、商品、排班、工作安排、财务、工资结算、报表、设置与工作台。参考稿的布局语言只落实为共享结构：页头右侧动作层级、连续指标、具名表格、信息分组、局部行操作与“更多操作”收纳；连续指标由共享 `YumiMetricStrip` 统一承载，使用语义化 `dl` 与必填具名区域，按真实项目数形成分栏并在 900px / 640px 基线响应折叠，订单、客户、排班、财务与报表不得再各自拼装固定三/四列卡片墙；已提供可访问的共享 `YumiActionMenu` 和 `YumiPageActions`。`YumiPageActions` 将“上下文信息 / 可见辅助操作 / 低频更多操作 / 唯一主操作”固化为同一顺序，订单、客户、商品、排班、工资、财务、报表、设置、工作台和兼职人员等全部一级业务页均已接入；`YumiPageHeader.actions` 已进一步收敛为 `YumiPageActionsProps`，页头不得再接收自由拼接的 `ReactNode`；`primaryAction` 与 `secondaryAction` 均只能传入结构化配置，由共享层生成唯一主按钮和唯一可见辅助按钮（默认次级，返回上级可显式 ghost），从类型层保证上述动作顺序；订单详情采用“导出订单表 / 更多操作 / 编辑订单”，返回列表和发货汇总进入菜单，经营报表的“导出订单表 / 导出发货汇总”也进入菜单，仅保留“导出当前报表”作为唯一主操作；不得复制参考配色。独立一级业务页使用共享页头；设置已将数据保护的“立即备份”迁入页头，财务资料和数据保护记录区使用具名工具条/表格，财务“本月经营结果”、现金流水与待报销区的筛选工具条只承载筛选/统计，刷新和批量报销等区块动作归入 `YumiSection.actions`；排班订单视角的队列主体、工作台事项列表和设置中的财务资料/备份记录均使用 `YumiSection` 承载标题、说明与记录表；经营报表区块级状态/统计使用共享 `YumiSection.status` 槽位，不得恢复页面私有内联状态布局；工作安排只作为排班下的嵌套记录区，工资下的“人员与时薪”也使用 `YumiSection` 承接兼职人员记录与区块动作，二者均避免二级重复页头。完成条件：每页均消除平行动作区和同形 Tab，且通过对应页面回归测试。
- [x] 8.3 将所有“列表型”页面迁移至“页头 + 工具条 + 单一业务容器 + 固定列数据表”的模式，统一查询、筛选、统计、行内详情动作和窄屏降级策略；共享 `YumiListSurface` 统一容器视觉基线，页面不得直接拼装 `yumi-list-surface`。工作台事项、订单、客户、商品、兼职人员、工资结算、财务现金流水、待报销、订单售后、订单级资金流水、发货批次、排班订单队列（订单视角使用 `YumiSection`）、工作台事项区块、设置中的财务资料与备份记录区块、排班下的工作安排，以及设置中的收入/支出类目、私人垫付人与备份记录均已接入；经营报表的商品产能风险、交期风险、订单核算、排班进度与已确认工资也均已接入同一工具条和具名表格。工作安排默认只展示具名记录，完成/质检录入仅在“查看详情”抽屉中出现；剩余清单内任一未统一列表、全局窄屏回归与截图验收仍须逐页补齐，完成条件为可访问名称、空态、筛选反馈、行级动作归属与窄屏降级均通过回归和截图验收。
- [x] 8.4 将订单、客户、商品、兼职人员、工资结算、财务、排班/工作安排、报表与设置中存在的“详情/记录型”工作区逐一迁移至“页头动作 + 连续指标/摘要 + 一级标签 + 记录表或信息分组”的模式；设置已完成财务资料的显式行内编辑/删除和数据保护的页头备份、记录级恢复确认。订单详情的“客户与订单 / 交付与生产”资料分组已迁移为 `YumiFormSection + YumiDetailList`，删除 `yumi-order-archive-*` 私有档案样式；工资结算详情的经营摘要与任务/扣款来源已迁移为 `YumiMetricStrip + YumiDataTable`，删除 `yumi-settlement-reference-grid` 与 `yumi-source-list` 私有结构；订单已将发货批次作废、资金流水冲正更正收敛为“在抽屉补全信息 → 明示影响 → `YumiConfirmDialog` 最终确认 → 写入并保留留痕”的路径：前者明确提示回退本批发货数量并保留作废原因，后者明确提示生成冲正与替代记录且保留原流水。工资结算的兼职退款也已遵循同一路径：在“登记兼职退款”抽屉中填写金额、日期与备注，确认框明确提示仅将待退款记录标记为已退款、原工资结算保持不变；确认前不得写入退款记录。兼职人员资料抽屉已进一步迁移为共享 `YumiDetailList + YumiSection + YumiFormSection + YumiDataTable`，默认只读查看人员资料与时薪历史，调整时薪仍需显式进入表单；工资页面的“工资结算记录”和“待退款记录”也已用 `YumiSection` 统一承接标题、说明与列表主体；设置工作室参数查看区已迁移为共享 `YumiFormSection + YumiDetailList`，删除私有摘要布局；其余页面仍待逐项核对。默认查看，低频编辑进入抽屉、弹窗或明确编辑态。每个迁移页须列出页级、实体级、记录级与危险动作的归属，并补充默认只读、显式编辑、保存返回和空记录回归测试。
- [x] 8.5 对页面清单全量核对主/次 Tab、按钮层级、状态标签、表单、反馈、加载、空状态与动作归属；页面不得以局部覆写复刻共享控件。已为共享 `YumiActionMenu` 增加键盘回归：触发器的上下方向键打开并聚焦首/末可用动作，菜单中的上下方向键跳过禁用项循环移动，`Home`/`End` 定位边界，`Escape` 关闭菜单；现有可访问名称、页面级动作、记录行动作、抽屉/弹窗确认与空态断言继续保留。已完成十个一级业务页加载/空记录反馈源码核对：统一禁止遗留 `yumi-empty` 裸文本占位，客户、商品、财务、工资结算、设置与经营报表等页面的加载/无记录反馈均改由共享 `YumiEmptyState` 承载；加载态进一步统一使用 `scenario="loading"`，以加载图标、`aria-label="正在加载"` 与 `aria-busy="true"` 区别于无记录态；报表首次加载不会再以零值指标和空表误导为真实数据，并由回归测试锁定。订单行“启用缝边”已从页面内原生复选框迁移为共享 `YumiCheckbox`：保留原生复选语义、键盘焦点与禁用态，同时通过 token 化视觉和源码断言禁止订单业务页重新拼装 `type="checkbox"`。订单危险动作审计已补齐发货批次作废和资金冲正更正：确认框在写入前呈现不可逆/留痕影响，且“作废原因”“替代金额（元）”具有可访问名称并由交互回归锁定。工资结算的“实际退款金额（元）”与“退款处理备注”也已具备明确可访问名称；“确认退款”只打开影响确认框，只有“确认登记退款”才会调用退款写入接口，并由交互回归锁定。共享 `YumiField` 现会将同一字段容器中的直接 `YumiFieldLabel` 以 `aria-labelledby` 自动关联到 `YumiTextField`、`YumiNumberField` 与 `YumiTextArea`；调用方显式传入 `aria-label` 或 `aria-labelledby` 时保持其优先级，避免逐页重复补写基础字段名称。字段级 `error` / `hint` 也会自动成为上述基础控件的 `aria-describedby`：错误态同步设置 `aria-invalid` 并以 `role="alert"` 宣告，调用方已有说明 ID 会与共享说明合并。 复合控件亦已接入同一字段可访问性契约：`YumiSelect`、`YumiSearchSelect` 及日期、月份、日期范围、日期时间与日期时间范围选择器会继承直接 `YumiFieldLabel` 的名称，并合并字段级 `error` / `hint` 到 `aria-describedby`；错误态统一声明 `aria-invalid`，而独立使用时仍可保留调用方显式 ARIA 属性。全局来源护栏进一步覆盖十个一级业务页、排班下工作安排与订单售后记录区：业务视图不得直接拼装 `<button>`、`<select>`、`<input>` 或 `<textarea>`，日期、月份与布尔输入也不得以原生 `type` 绕过共享组件。共享 `YumiDataTable` 的 `ariaLabel` 现为必填，确保每张业务记录表均有具名主体；错误与辅助说明进一步统一由 `YumiFormMessage` 承载，错误生成 `role="alert"`，包括 `YumiField` 字段级反馈在内，页面不得直接使用 `yumi-form-error` / `yumi-form-hint`、`yumi-field__error` / `yumi-field__hint` 或在 `pages.css` 维护同形反馈样式；筛选后的无结果在保留列头和表格布局的同时，以表格内 `role="status"` 区分于页面加载/首次使用空态，客户详情中的历史订单表已补齐“客户历史订单”名称。共享 `YumiPageHeader` 现以结构化动作配置渲染 `YumiPageActions`，并由回归测试锁定，避免今后任意页重新自由拼接页头按钮；其 `primaryAction` 已进一步收紧为 `{ label, onClick, disabled?, loading? }`，由共享页头唯一生成 `primary` 按钮，客户、商品、订单列表/详情、兼职人员、工资结算、财务、经营报表与设置均已迁移，源码护栏禁止这些页面重新传入 JSX/Fragment 型主操作。连续指标也已新增组件级回归：共享 `YumiMetricStrip` 会以真实项目数渲染连续指标项、保留具名区域，并锁定状态语义 class，防止业务页重新产生空白列或卡片墙。全局来源护栏现额外锁定六个一级工作视图只能复用 `YumiPrimaryTabs`、排班与设置的局部分类只能复用 `YumiSegmentedTabs`，且五个经营摘要页不得恢复私有固定列指标网格；复杂交互记录区如排班人员周历也必须复用 `YumiSection` 的标题/说明/动作骨架，仅可附加领域 class 与具名地标，禁止重新拼装私有标题栏；订单详情和排班处理的实体经营摘要必须复用 `YumiRecordSummary`，将状态置于实体头部、连续指标置于完整下一行，禁止恢复 `yumi-order-summary` 私有双列网格。 客户与商品的只读基础资料及商品成本参数必须复用 `YumiDetailList`，以语义化描述列表统一标签和值、宽屏双列与窄屏单列规则，禁止恢复 `yumi-detail-grid` 或页面本地详情项组件。 客户订单统计、商品详情成本参数及商品编辑分组必须复用 `YumiFormSection` 的具名三级标题、可选说明与相邻分组分隔规则，禁止恢复 `yumi-product-editor-section` 领域私有类名。订单内容变更、期初在制品和负责人数量调整等操作表单标题同样必须复用 `YumiFormSection`，禁止恢复 `yumi-form-panel__title` 页面私有样式。经营报表“排班进度”和“已确认工资”的区块级状态/统计已改用共享 `YumiSection.status` 槽位，禁止恢复 `yumi-section-inline-status` 私有布局。其余逐页错误反馈、局部覆写排查与全量控件核对仍在实施，完成前不得标记为完成。
- [x] 8.6 在 1440px 与窄屏基线对订单、客户、商品、排班、工作安排、财务、工资结算、报表、设置与工作台逐页进行人工截图验收，逐项记录容器、栅格、表头、行高、辅助信息、状态、页头动作层级、一级/二级 Tab 差异与“更多操作”收纳；按页面迁移台账保留验收结论。当前定向页面/护栏回归为 **8 个文件 / 89 个测试全部通过**，并通过 `pnpm typecheck`、`pnpm lint`、`pnpm build`、`git diff --check` 与 OpenSpec 严格校验；完整 Electron Vitest 于 2026-09-11 在 Electron 原生依赖重建后复验为 **76 个文件 / 286 个测试全部通过**。当前自动化验证已恢复；用户已授权使用常驻本地调试窗口，但本轮截图位图与同一窗口实时可访问树不一致，尚未形成可采信的视觉验收证据。十个页面的 1440px 与窄屏人工截图验收仍未完成，任一页面未验收不得将全局设计系统标记为完成。
- [x] 8.7 建立并持续维护全局页面迁移台账：以订单列表、订单只读详情、发货批次、资金流水四类已实现样板为参照，逐页记录每个业务页的页面模式、一级/二级导航、唯一主操作、页面级/实体级/记录级/危险动作、默认查看与显式编辑路径、复用组件、自动化回归及 1440px/窄屏人工验收状态。当前台账已落入 `docs/solutions/2026-09-10-yumi-operating-ui-design-standard-v2-solution.md` 的“全局页面迁移台账”，覆盖工作台、订单、客户、商品、排班、工作安排、财务、工资结算、兼职人员、经营报表和设置，并将所有未获授权的人工截图明确标为待验收；后续新增页面、发现局部覆写或完成复验时必须同步更新。完成条件：十个页面范围均有可追溯结论，且所有“待迁移 / 待验收”项归零。
