## 1. 桌面应用骨架与安全边界

- [x] 1.1 使用 Electron、React、TypeScript 和 Radix Themes 建立桌面应用工程，配置主进程、预加载脚本、渲染进程及开发/打包命令。
- [x] 1.2 在 `src/main/index.ts` 配置安全 BrowserWindow：启用上下文隔离、关闭 Node 集成，并仅加载受控的预加载脚本。
- [x] 1.3 在 `src/preload/index.ts` 定义按业务模块划分的只读 `window.yumi` IPC API，并为渲染进程提供 TypeScript 声明。
- [x] 1.4 建立 `src/main/{catalog,orders,payments,scheduling,production,analytics,backup,audit}` 与共享 `domain`、`repositories`、`database` 目录边界，禁止页面直接访问 SQLite。
- [x] 1.5 配置 ESLint、格式化、单元测试和 Electron 进程测试基础设施，并验证空应用可启动、测试命令可执行。

## 2. 本地数据、迁移与审计基础

- [x] 2.1 在 `src/main/database` 实现 SQLite 连接、启动迁移、事务执行和应用数据目录定位，确保数据库仅由主进程访问。
- [x] 2.2 设计并创建客户、商品、商品成本参数、订单、订单明细、收退款、兼职人员、排班、排班任务、实际完成、系统设置、附件索引和审计日志的迁移表及必要索引。
- [x] 2.3 为仓储层建立明确的实体、输入 DTO 和查询 DTO，统一金额、日期、时长、数量和状态字段的校验及序列化规则。
- [x] 2.4 实现关键写操作的事务边界：订单及其快照、收退款、排班与任务、实际完成、恢复操作必须在失败时整体回滚。
- [x] 2.5 实现审计日志写入器，记录收退款、订单价格或优惠、商品产能参数、风险确认、缺勤和数据恢复的操作者、前后值、时间与关联业务 ID。
- [x] 2.6 实现附件文件安全存储与索引；收退款凭证和商品图片删除时应同步处理本地文件及索引。

## 3. 商品、成本与系统参数

- [x] 3.1 实现商品分类和商品资料的仓储及领域服务，支持名称、编码、启用状态、基础售价、图片和商品级缝边单价维护。
- [x] 3.2 实现商品制作与成本参数维护：单件重量、损耗率、标准制作分钟、包装单价、固定单件提成、模具数量、每模每批产出及手填每日批次数。
- [x] 3.3 实现胶水克单价、月度房租水电与目标有效工时等系统成本设置，并保存生效时间和变更审计。
- [x] 3.4 实现成本预估领域计算：胶水克数与成本、包装、标准工时、固定费用分摊、固定按件提成、缝边收入及预计成本合计。
- [x] 3.5 实现商品每日最大制作数量计算，并为排班服务提供按商品与日期查询的物理产能接口。
- [x] 3.6 为商品成本计算、损耗、每日产能和非法参数输入编写单元测试。

## 4. 客户、订单与收退款管理

- [x] 4.1 实现轻量客户档案服务，支持昵称或姓名、联系方式、常用地址、备注及关联订单历史查询。
- [x] 4.2 实现订单创建与编辑流程，支持多个订单明细、成交单价、缝边选择和数量、优惠或改价、预计发货日期及订单级预留天数。
- [x] 4.3 在订单创建或商品明细变更时生成并保存客户、商品售价、成本和制作参数快照，确保后续默认参数变更不改写历史订单成本。
- [x] 4.4 实现订单应收、收款合计、退款合计、已收净额、待收金额及未收款、部分收款、已结清、退款中、已退款、超收等资金状态计算。
- [x] 4.5 实现可追加的收款和退款流水服务，支持日期、金额、方式、备注、可选凭证与审计记录，并在同一事务内刷新订单资金汇总。
- [x] 4.6 实现订单制作状态流转和最晚完成制作日期计算，保持制作状态与资金状态相互独立。
- [x] 4.7 为订单快照、多商品金额、分次收款、部分退款、余额状态和预留天数边界编写单元及集成测试。

## 5. 兼职人员、日历排班与产能风险

- [x] 5.1 实现兼职人员资料、时薪和默认工作时间规则维护，并保留历史时薪变更所需的数据边界。
- [x] 5.2 实现按人员、日期、起止时间创建的排班实体，以及一个排班包含多个订单商品任务的持久化与查询接口。
- [x] 5.3 实现排班任务的计划数量和预计分钟计算，并将任务与订单明细、商品物理产能和订单最晚完成日期关联。
- [x] 5.4 实现 `schedule.previewShift` 风险预览，识别人员时间重叠、排班任务超出时段、时段未充分排满、商品当日模具产能超限及超过制作截止日期。
- [x] 5.5 实现 `schedule.saveShift(input, confirmedWarningCodes)`，对业务风险弹出确认后允许保存，保存风险确认代码和审计日志；非法引用、无效时间和无效数量仍必须拒绝写入。
- [x] 5.6 实现缺勤、请假、迟到、取消和完成等排班状态，以及缺勤后未完成计划数量转为待补排并重新计算订单交期风险的逻辑。
- [x] 5.7 为同人员重叠、超时与未排满提示、同商品跨订单共享日产能、风险确认保存和缺勤补排编写集成测试。

## 6. 实际制作、人工成本与提成结算

- [x] 6.1 实现排班任务实际开始/结束、实际分钟、合格完成数量、返工数量和报废数量的记录服务，并校验与原排班任务的关联。
- [x] 6.2 实现实际人工时薪成本计算，使用实际分钟与该次制作对应的人员时薪记录，避免之后调整时薪改写历史成本。
- [x] 6.3 实现按订单商品快照固定单价和合格完成数量计算的按件提成，明确返工与报废不计入合格完成提成。
- [x] 6.4 将实际材料、人工、提成及已知成本汇总到订单和报表查询，并在状态变化时刷新待补排与制作进度。
- [x] 6.5 为实际分钟、合格数量、返工报废和按件提成的计算与边界条件编写单元测试。

## 7. 桌面工作区与业务页面

- [x] 7.1 搭建温暖版 Linear 风格的桌面外壳：窄侧栏、命令栏、内容表格区和右侧检查器，避免营销式网页大卡片布局。
- [x] 7.2 实现概览工作区，展示待收款、临近发货、待补排、模具超载和未确认风险的可点击工作队列。
- [x] 7.3 实现商品工作区：商品列表、详情检查器、成本参数表单、成本预估与每日模具产能预览。
- [x] 7.4 实现订单工作区：客户快速选择或新建、订单多商品编辑、缝边选项、成本与应收预览、预计发货和预留时间维护、收退款时间线。
- [x] 7.5 实现周视图人员资源日历和月度产能风险视图；在创建或编辑排班时先展示风险确认对话框，再允许用户确认保存。
- [x] 7.6 实现兼职人员工作区，提供人员资料、时薪、默认工作时间、历史排班、实际工时和提成汇总。
- [x] 7.7 实现排班详情检查器，支持标记缺勤/请假和录入实际制作结果，并清晰显示可能需要补排的数量。
- [x] 7.8 为主要页面补齐加载、空状态、输入错误、不可恢复操作二次确认和键盘可访问性处理。

## 8. 报表、备份、恢复与导出

- [x] 8.1 实现订单资金与利润报表查询，支持日期、订单状态和待收条件筛选，并区分预计与实际成本及利润。
- [x] 8.2 实现兼职人员结算报表，按人员与日期范围汇总实际工时、合格完成数量、时薪成本、按件提成和缺勤次数。
- [x] 8.3 实现商品成本与产能报表，以及未完成订单的交期风险列表，展示每日计划数量、产能、待补排和风险原因。
- [x] 8.4 实现业务数据 XLSX 导出，限定导出内容为业务表格，不将数据库文件暴露为普通导出。
- [x] 8.5 实现 SQLite 数据库、附件和版本元数据的本地备份与恢复；恢复前自动创建当前备份、显示覆盖提示，并要求二次确认。
- [x] 8.6 实现设置与备份页面，展示最近备份、恢复结果、导出结果和可查询的关键操作审计记录。
- [x] 8.7 为报表汇总、XLSX 列结构、备份完整性、恢复前保护备份和恢复审计编写自动化测试。

## 9. 端到端验收与发布准备

- [x] 9.1 建立一套演示数据，覆盖多商品订单、分次付款和退款、不同缝边单价、模具超载、请假缺勤及待补排场景。
- [x] 9.2 编写端到端验收流程：创建商品和成本参数、创建带交期的订单、记录两次收款、创建日历排班并确认风险、登记实际完成、核对提成和报表。
- [x] 9.3 验证所有风险均为确认提示而非保存阻断，同时验证无效时间、无效金额、无效数量和无效关联 ID 被拒绝。
- [x] 9.4 验证应用在断网环境下可启动和完整使用，且不监听 HTTP 服务端口。
- [x] 9.5 验证生产打包后的数据库迁移、附件目录、备份恢复和 Windows/macOS 目标平台应用数据目录行为。

## 实施记录

- 2026-09-05：已完成 7.4。订单工作区支持新建与编辑多商品订单，保留订单商品明细 ID 以兼容既有排班，维护客户、成交单价、缝边数量/单价、明细优惠、订单优惠、预计发货、预留天数和备注；新增 `orders.update` 受控 IPC 与事务更新，编辑会重新生成商品成本快照并保护已有收退款与制作关联；订单检查器增加“编辑订单”，并继续提供应收、预计/实际成本和分次收退款时间线。新增订单编辑集成测试。验证证据：订单管理测试通过。
- 2026-09-04：推进 7.2 的概览工作队列切片。概览现在提供待收款、临近发货、待补排和排班风险四个可点击入口，并将待处理订单列表与订单检查器联动；由于当前数据层还未持久化“尚未保存的排班风险”状态，7.2 暂不勾选，待补齐未确认风险查询后再完成。
- 2026-09-04：已完成 7.6。兼职人员列表可打开桌面式人员工作区，维护姓名、联系电话、时薪、默认上班时间、默认下班时间、生效日期和在岗状态；新增 `workers.get` 受控 IPC，展示时薪历史、历史排班、实际工时、合格数量、缺勤/请假次数和按件提成汇总，并补充人员详情聚合测试。
- 2026-09-04：已完成 1.5、3.1、3.2、3.3、3.6。新增商品详情与更新契约、图片路径/备注/启停维护、成本设置历史表和本机管理员审计；补齐 Vitest 别名解析。验证证据：`npm run format:check`、`npm run typecheck`、`npm test`（5 个测试文件、9 个测试通过）、`npm run lint`、`npm run build` 和 `openspec validate yumi-studio-v1-desktop-management --strict` 均成功。
- 2026-09-04：已完成 1.1、1.2、1.3、1.4、2.1、2.2、3.4、3.5、7.1。验证证据：`npm run format:check`、`npm test`（6/6 通过）、`npm run typecheck`、`npm run lint`、`npm run build` 均成功。
- 2026-09-04：Electron 34.5.8 二进制已通过 `ELECTRON_MIRROR=https://npmmirror.com/mirrors/electron/` 修复；下载包 SHA-256 与 `checksums.json` 一致。`CSC_IDENTITY_AUTO_DISCOVERY=false ELECTRON_MIRROR=https://npmmirror.com/mirrors/electron/ npx electron-builder --mac --dir` 成功生成未签名测试包，并通过 LaunchServices 启动，确认主进程、GPU、网络与 Renderer 进程均已创建，SQLite 已创建且迁移版本为 `initial_schema`。自动化 shell 中直接运行 `npx electron --version` 的 SIGABRT 为 macOS LaunchServices 上下文限制，不是二进制缺失。
- 2026-09-04：已完成客户、订单与收退款基础能力以及日历排班风险能力：客户订单历史、多商品订单、售价与成本快照、预留天数、资金状态、分次收退款、制作状态流转、排班风险预览、确认后保存、兼职缺勤与待补排。验证证据：新增 `src/main/services/order-management.test.ts` 和 `src/main/services/scheduling-management.test.ts`，全量 `npm test`（7 个测试文件、15 个测试通过）、`npm run typecheck`、`npm run lint`、`npm run format:check`、`npm run build` 均成功。
- 2026-09-04：补齐兼职人员默认工作时间、启停维护和时薪历史版本；排班集成测试覆盖人员时间重叠、工时超载/未排满、同商品跨订单日产能、风险确认保存及缺勤待补排。
- 2026-09-04：完成实际制作记录、历史时薪匹配、合格数量按件提成和订单实际人工/提成成本回写；新增实际制作成本与结算边界测试。
- 2026-09-04：实际制作记录会按订单聚合人工与按件提成成本，并在全部商品合格完成后自动进入待发货，否则进入制作中。
- 2026-09-04：推进 7.4 与 7.5 的首个可用桌面交互切片（尚未勾选，订单编辑、成本详情及月度产能风险视图仍待实施）。订单页现支持客户快速选择或新建、多商品明细、商品级缝边数量/单价、逐项及订单优惠、预计发货/预留天数、应收预览；订单检查器现支持查看快照、成本和可追加的分次收退款时间线。周历现可直接按人员和日期创建最终时间段、选择多个订单商品任务、预览重叠/工时/模具/交期风险并显式确认后保存。新增 `workspace-utils` 的金额与周日期单测。验证证据：全量 `npm test`（11 个测试文件、23 个测试通过）、`npm run format:check`、`npm run typecheck`、`npm run lint`、`npm run build`、`openspec validate yumi-studio-v1-desktop-management --strict` 均成功。
- 2026-09-04：按 OpenSpec 实际完成状态校正任务清单：已勾选 2.3。订单编辑切片完成后，4.2 与 7.4 已在后续记录中更新为已完成。主进程服务层已用 Zod 对 DTO 统一校验，领域层与共享契约统一金额（分）、日期、时间、数量与状态序列化；仓储查询 DTO 已由共享契约约束。2.4、2.5、2.6、4.2、7.2–7.8 等仍因恢复、附件、编辑、月视图或完整桌面交互尚未完成而保持未勾选，避免虚报进度。
- 2026-09-04：已完成 7.7。周历中的既有班次现在可直接打开排班详情检查器，查看订单号、商品、计划与实际工时、合格/返工/报废数量、实际人工与按件提成成本及待补排数量；支持标记请假、缺勤、迟到、取消、完成，且请假/缺勤/取消前会进行明确确认；可在检查器内录入或修正实际制作结果。主进程新增受控 `schedule.get` IPC，并补充排班详情返回订单、商品、实际成本和待补排数量的服务测试。
- 2026-09-04：已完成 7.3。商品列表可打开桌面详情检查器，维护启停、售价、缝边单价、制作参数、包装、固定按件提成、模具与每日批次、图片路径和备注；成本预估通过受控 `products.previewCost` IPC 在主进程按当前胶水与固定成本设置计算胶水、包装、人工、提成、房租水电分摊、缝边收入、预计成本/收入/毛利和每日模具产能。新增该成本预览服务测试。
- 2026-09-05：推进 7.5 的月度产能风险视图切片。排班工作区新增周视图/月度风险切换；月视图按日期聚合排班详情中的商品计划数量，与商品每日模具日产能对比并显示超载明细，支持跨月浏览且以周一为起始日。7.5 暂不勾选：既有排班时间段与任务的编辑流程尚未独立完成，新建排班的风险预览与确认已具备。验证证据：`npm run format:check`、`npm run typecheck`、`npm run lint`、`npm test`（11 个测试文件、27 个测试通过）、`npm run build`、`openspec validate yumi-studio-v1-desktop-management --strict` 均成功。
- 2026-09-05：已完成 2.4。订单创建的客户、订单与明细快照写入、收款凭证存在性校验、排班与任务写入均在 SQLite 事务边界内执行；新增事务边界集成测试，验证订单明细失败会回滚自动创建的客户和订单、无效收款凭证不会留下半条收款流水、无效排班任务不会留下班次或任务。实际制作记录与恢复操作沿用现有事务封装。验证证据：`src/main/services/transaction-boundaries.test.ts`（3 个测试通过）、全量 `npm test`（12 个测试文件、31 个测试通过）、`npm run format:check`、`npm run typecheck`、`npm run lint`、`npm run build`、`openspec validate yumi-studio-v1-desktop-management --strict` 均成功。
- 2026-09-05：已完成 2.5。审计写入统一保存操作类型、实体类型与关联 ID、固定本机操作者、前值、后值、元数据和 ISO 时间；商品创建/产能参数变更、订单价格/优惠、收退款、风险确认、缺勤状态变更均写入审计。新增可复用的 `recordAudit` 入口，供数据恢复流程记录恢复前后摘要与备份关联信息；新增详细审计读取以供后续审计工作区使用。新增审计集成测试覆盖上述场景。验证证据：`src/main/services/audit-management.test.ts`、全量 `npm test`（13 个测试文件、32 个测试通过）、`npm run format:check`、`npm run typecheck`、`npm run lint`、`npm run build`、`openspec validate yumi-studio-v1-desktop-management --strict` 均成功。
- 2026-09-05：已完成 7.2。概览工作队列继续提供待收款、临近发货、待补排和排班风险四个可点击入口；新增第 5 版迁移，将排班预览得到的完整风险明细持久化到 `shifts.detected_risks_json`，概览风险汇总不再只依赖确认码，重启后仍可查询。新增排班集成断言覆盖风险汇总。验证证据：全量 `npm test`（13 个测试文件、32 个测试通过）、`npm run format:check`、`npm run typecheck`、`npm run lint`、`npm run build`、`openspec validate yumi-studio-v1-desktop-management --strict` 均成功。
- 2026-09-05：已完成 7.5。排班工作区支持周视图和以周一为起始日的月度产能风险视图；月视图按商品和日期汇总排班计划，与模具日产能比较并显示超载。既有待执行排班可从详情检查器进入编辑，编辑时会排除当前班次自身的重叠/日产能影响，重新预览风险并在确认后保存；已登记实际制作结果或非待执行状态的排班拒绝编辑。新增排班编辑集成测试，覆盖风险重新确认、更新任务数量、审计记录及实际制作后的编辑保护。验证证据：`npm run format:check`、`npm run typecheck`、`npm run lint`、`npm test`（11 个测试文件、28 个测试通过）、`npm run build`、`openspec validate yumi-studio-v1-desktop-management --strict` 均成功。

- 2026-09-05：已完成 2.6。新增受控附件目录与附件索引：仅允许图片或 PDF 凭证、限制单文件 25 MB、采用随机文件名复制到应用数据目录，数据库索引写入失败会删除已复制文件。商品详情和订单收退款检查器均可通过原生文件选择器导入附件；收退款保存时引用凭证 ID，关闭未提交表单或替换商品图片时清理暂存附件，保存商品更换或移除图片时同步清理旧图片文件及索引。新增附件集成测试覆盖导入、索引、类型拦截、手动删除和商品图片替换/移除。验证证据：全量 `npm test`（14 个测试文件、36 个测试通过）、`npm run format:check`、`npm run typecheck`、`npm run lint`、`npm run build`、`openspec validate yumi-studio-v1-desktop-management --strict` 均成功。

- 2026-09-05：已完成 7.8。应用主工作区增加本地数据加载中的状态、读取失败提示和重试入口；统一未知异常到用户可读错误信息；商品、订单、兼职人员列表的可点击行支持 Tab 聚焦、Enter/空格打开检查器，并补充 `aria-label`、`aria-current`、`aria-live`、`role=alert/status` 与焦点可视样式；商品图片移除增加二次确认，保留主要列表和表单的空状态与输入错误展示。新增工作区错误信息辅助函数测试。验证证据：全量 `npm test`（14 个测试文件、37 个测试通过）、`npm run format:check`、`npm run typecheck`、`npm run lint`、`npm run build`、`openspec validate yumi-studio-v1-desktop-management --strict` 均成功。
- 2026-09-05：已完成 8.1。新增订单资金与利润报表查询服务与受控 IPC，支持预计发货日期、制作状态和待收条件筛选；报表展示应收、已收净额、待收、预计/实际成本及预计/实际利润，桌面报表工作区提供筛选、加载、空状态、错误和汇总。新增报表集成测试，覆盖日期与待收筛选、资金状态、预计/实际利润及非法日期校验。验证证据：全量 `npm test`（15 个测试文件、39 个测试通过）、`npm run format:check`、`npm run typecheck`、`npm run lint`、`npm run build` 均成功。
- 2026-09-05：已完成 8.2。新增兼职人员结算报表查询服务与受控 IPC，按排班日期范围汇总实际工时、合格完成数量、实际时薪成本、按件提成和请假/缺勤次数；报表工作区新增订单利润与人员结算切换、日期筛选、加载/错误/空状态、汇总卡片和桌面表格。新增结算报表集成测试，覆盖人员日期汇总和非法日期校验。验证证据：全量 `npm test`（16 个测试文件、41 个测试通过）、`npm run format:check`、`npm run typecheck`、`npm run lint`、`npm run build` 均成功。
- 2026-09-05：已完成 8.3。新增商品单位成本与日期产能查询、未完成订单交期风险识别及受控 IPC；报表工作区新增产能风险视图，展示商品成本/日产能/期间计划与完成、每日计划与模具容量、待补排数量及订单风险原因。新增产能风险集成测试，覆盖单位成本、日产能、计划完成数量、待排风险和非法日期校验。验证证据：全量 `npm test`（17 个测试文件、43 个测试通过）、`npm run format:check`、`npm run typecheck`、`npm run lint`、`npm run build` 均成功。
- 2026-09-05：已完成 8.4。引入 `xlsx` 依赖，新增订单资金、兼职人员结算、商品产能/每日计划/交期风险三类业务报表导出；导出通过主进程保存对话框写入用户指定 `.xlsx` 文件，普通导出只包含业务列，不暴露 SQLite 文件、内部 JSON 快照或数据库表结构。报表页面新增导出 XLSX 入口。新增 XLSX 集成测试验证工作表名称、业务列和内部字段隔离。验证证据：全量 `npm test`（18 个测试文件、44 个测试通过）、`npm run format:check`、`npm run typecheck`、`npm run lint`、`npm run build` 均成功。

- 2026-09-05：已完成 8.5。新增校验型本地备份服务，使用 SQLite 在线快照生成包含数据库、附件文件清单 SHA-256 校验、格式版本和应用版本元数据的可识别备份文件夹；恢复前会校验备份完整性、自动创建恢复前保护备份，并以临时文件与可回滚替换方式恢复数据库和附件。新增受控备份 IPC：可立即备份、列出备份、选择并校验恢复源；恢复要求渲染进程确认和原生覆盖警告的双重确认，成功后写入恢复审计并自动重启应用。新增备份恢复集成测试，覆盖版本元数据、嵌套附件、恢复前保护备份、二次确认、数据/附件恢复和恢复审计。验证证据：全量 `npm test`（19 个测试文件、45 个测试通过）、`npm run format:check`、`npm run typecheck`、`npm run lint`、`npm run build`、`openspec validate yumi-studio-v1-desktop-management --strict` 均成功。

- 2026-09-05：已完成 8.6。设置与备份工作区改为桌面化分栏管理界面，可创建/选择本地备份、显示最近备份和恢复时间、通过页面确认后进入原生覆盖确认；恢复与导出结果会以本地活动元数据跨重启保留。新增最近导出摘要和可刷新关键操作审计表，主进程新增受控活动状态、审计查询 IPC，业务报表导出后同步记录导出结果。验证证据：全量 `npm test`（19 个测试文件、45 个测试通过）、`npm run format:check`、`npm run typecheck`、`npm run lint`、`npm run build`、`openspec validate yumi-studio-v1-desktop-management --strict` 均成功。

- 2026-09-05：已完成 8.7。补强并核验报表与本地数据保护自动化覆盖：订单利润报表测试已断言汇总金额，XLSX 测试固定断言订单资金报表完整业务列及内部字段隔离；备份恢复测试新增附件篡改完整性拒绝，且覆盖恢复前保护备份、二次确认、数据库/嵌套附件恢复、恢复审计以及跨重启展示所需的导出/恢复活动状态。验证证据：全量 `npm test`（19 个测试文件、45 个测试通过）、`npm run format:check`、`npm run typecheck`、`npm run lint`、`npm run build`、`openspec validate yumi-studio-v1-desktop-management --strict` 均成功。

- 2026-09-05：已完成 9.1。新增受控演示数据服务，仅允许空白业务数据库加载，避免覆盖真实数据；设置与备份工作区提供“加载演示数据”入口，并在加载后刷新全局数据。演示数据覆盖 2 个不同缝边单价的商品、含多商品明细的订单、两次收款和一次退款、低模具日产能商品的超载风险确认、缺勤后待补排任务，以及实际制作产生的人工与按件提成。新增演示数据集成测试，覆盖上述业务场景和已有数据保护。验证证据：全量 `npm test`（20 个测试文件、47 个测试通过）、`npm run format:check`、`npm run typecheck`、`npm run lint`、`npm run build`、`openspec validate yumi-studio-v1-desktop-management --strict` 均成功。

- 2026-09-05：已完成 9.2。新增端到端验收集成测试，完整串联成本设置、商品创建、带交期和预留天数的订单、两次收款、日历时间段排班风险预览与确认、实际制作登记、班次完成，以及订单利润和兼职人员结算报表核对；测试固定断言实际人工成本和按件提成金额。验证证据：全量 `npm test`（21 个测试文件、48 个测试通过）、`npm run format:check`、`npm run typecheck`、`npm run lint`、`npm run build`、`openspec validate yumi-studio-v1-desktop-management --strict` 均成功。

- 2026-09-05：已完成 9.3。新增风险与输入验证验收测试，覆盖人员时间重叠、时段工时超载、时段未充分排满、模具日产能超限、超过制作截止日期五种风险；每种风险均验证先提示、确认风险代码后可保存。测试同时验证结束早于开始的时间、零金额、零数量及不存在的商品关联 ID 均会被领域服务拒绝写入。验证证据：全量 `npm test`（22 个测试文件、50 个测试通过）、`npm run format:check`、`npm run typecheck`、`npm run lint`、`npm run build`、`openspec validate yumi-studio-v1-desktop-management --strict` 均成功。

- 2026-09-05：已完成 9.4。重新打包的 macOS 应用以 Chromium host resolver 将外部主机解析阻断后成功启动，隔离应用数据目录中创建了本地 SQLite 数据库；对该应用进程的 TCP LISTEN 检查为空。主进程、预加载和渲染源码扫描未发现 HTTP 服务监听或远程请求实现；全量自动化验收均在不依赖网络服务的本地 SQLite 环境完成。验证证据：`CSC_IDENTITY_AUTO_DISCOVERY=false npx electron-builder --mac --dir`、host-resolver 阻断启动、`lsof -nP -a -c 'YUMI Studio' -iTCP -sTCP:LISTEN`、全量 `npm test`（22 个测试文件、50 个测试通过）、`npm run format:check`、`npm run typecheck`、`npm run lint`、`npm run build`、`openspec validate yumi-studio-v1-desktop-management --strict` 均成功。

- 2026-09-05：已完成 9.5。已生成未签名 macOS arm64 `.app` 与 Windows x64 `win-unpacked` 生产包；两个包均包含 `app.asar` 及按目标平台解包的 `better-sqlite3` 原生模块（macOS 为 arm64 Mach-O，Windows 为 x64 PE）。macOS 生产包在隔离 `userData` 目录启动后完成 1 至 5 号 SQLite 迁移；两个生产包内的主进程均使用 Electron `app.getPath('userData')`，并以该目录派生数据库、`attachments` 和 `backups` 路径。附件导入、备份完整性、恢复前保护备份、数据库与附件恢复的自动化测试通过。验证证据：`CSC_IDENTITY_AUTO_DISCOVERY=false npx electron-builder --mac --dir`、`CSC_IDENTITY_AUTO_DISCOVERY=false npx electron-builder --win --x64 --dir`、生产包内容与原生模块检查、迁移查询、附件/备份恢复测试、全量 `npm test`（22 个测试文件、50 个测试通过）、`npm run format:check`、`npm run typecheck`、`npm run lint`、`npm run build`、`openspec validate yumi-studio-v1-desktop-management --strict` 均成功。
