# 任务：YUMI 商品、工序、工资与公式中心重构

关联方案：`docs/solutions/2026-09-14-product-process-payroll-and-formula-center-solution.md`（v1.1）
提案名称：YUMI 商品、工序、工资与公式中心重构
Change ID：`redesign-product-process-payroll-and-costing`
创建人：Codex
状态：已完成实施，待授权归档

本提案只使用一个 change，并按以下四个阶段顺序实施。每项任务必须先建立失败测试或确认现有失败行为，再完成最小实现；任务只有在目标行为和列出的验证同时通过后才能勾选。不得增加旧业务数据迁移或旧模型兼容分支。

## 1. 阶段 A：统一公式中心与金额公式组件

- [x] 1.1 复核已归档 `2026-09-14-yumi-core-capability-recovery` 生效后的主规格，以及 `src/shared/money/index.ts`、`src/main/domain/order-amounts.ts`、`src/main/domain/product-costing.ts`、`src/main/domain/settlement.ts` 和 `src/renderer/pages/orders/index.tsx` 的当前计算调用链；记录仍须保留的订单级缝边与订单金额行为，并为金额精度、订单金额和现有材料金额增加特征测试。完成条件：基线行为有测试保护且未回退已归档提案成果；验证：相关 domain 测试与 `git diff --check`。
  - 证据：`npx vitest run src/shared/money src/main/domain/order-amounts.test.ts src/main/domain/product-costing.test.ts src/main/domain/settlement.test.ts src/main/domain/scheduling.test.ts src/main/domain/fulfillment.test.ts` 退出码 0（6 文件 32 测试通过）；`git diff --check` 干净。保留口径：明细「商品金额 + 缝边金额 − 明细优惠」、订单优惠上限、未勾选缝边归零且默认全量可部分、整数分 HALF_UP 边界舍入、材料按克单价×毫克×数量一次性舍入。
- [x] 1.2 新增 `src/shared/calculations/types.ts` 与 `src/shared/calculations/expression.ts`，定义金额/比例计算结果、受控表达式节点、统一舍入、公式文本、代入文本和组成明细；覆盖零值、负利润、除数为零、求和及精度测试。完成条件：表达式不执行字符串且相同输入稳定产生数值与展示数据；验证：`src/shared/calculations/expression.test.ts`。
  - 证据：`npx vitest run src/shared/calculations/expression.test.ts src/shared/money/money.test.ts` 退出码 0（20 测试通过，覆盖求和、负利润、除数为零不可计算、万分比 HALF_UP、材料一次性舍入、稳定输出与未知节点拒绝）；`pnpm run typecheck` 退出码 0。
- [x] 1.3 新增 `src/shared/calculations/material-cost.ts`、`order-amount.ts`、`wage.ts` 和稳定出口 `index.ts`，将材料成本、订单金额、分钟时薪和比例金额组合迁入共享计算层，并让既有 main 调用方委托共享函数。完成条件：main 不再维护第二套同义公式且整数金额结果不变；验证：shared calculation 测试、`src/main/domain/order-amounts.test.ts`、`src/shared/money/money.test.ts`。
  - 证据：`npx vitest run src/main/services src/main/repositories src/main/domain src/shared` 退出码 0（31 文件 140 测试通过）；`src/main/domain/order-amounts.ts` 改为直接重导出 `@shared/calculations/order-amount`（新增恒等断言固化）；`settlement.ts` 计时工资与 `product-costing.ts` 材料成本委托共享函数；`pnpm run typecheck` 退出码 0。shared 模块同时新增 `@shared/errors`（DomainValidationError 上移，main 重导出）。
- [x] 1.4 新增 `src/shared/calculations/catalog.ts`，将公式标识、业务名称、适用范围和展示表达式注册为统一目录；修改 `src/renderer/pages/settings/index.tsx` 删除本地硬编码公式行。完成条件：设置页公式目录完全来自共享注册表；验证：公式目录单测和设置页 renderer 测试。
  - 证据：`npx vitest run src/shared/calculations/catalog.test.ts src/renderer/pages/settings/formula-catalog.test.ts src/renderer/pages/reference-pages.test.tsx` 退出码 0（16 测试通过，含源码守卫「不再存在 calculationFormulaRows」与逐行渲染断言）；`pnpm run typecheck` 退出码 0。
- [x] 1.5 新增 `src/renderer/components/ui/calculated-amount/yumi-calculated-amount.tsx`、出口和组件测试，实现结果、公式主干、代入值、组成明细、负值语义、不可计算比例和 `aria-label`；在 `src/renderer/components/ui/index.ts` 暴露组件并按现有样式体系补充必要样式。完成条件：组件不接收页面手写公式字符串且键盘/屏幕阅读器可理解结果；验证：`yumi-calculated-amount.test.tsx`、样式层级测试。
  - 证据：`npx vitest run src/renderer/components/ui/calculated-amount src/renderer/styles` 退出码 0（6 + 21 测试通过，覆盖业务名称/金额/公式主干/代入值/aria-label、利润正负色调与负号文本、不可计算比例占位符、组成明细按需展开）；`pnpm run typecheck` 退出码 0。
- [x] 1.6 修改 `src/renderer/pages/orders/index.tsx` 及相关 composable，让订单草稿金额预览调用共享订单公式，并在 main service 保存边界重新计算；拒绝信任 renderer 派生总额。完成条件：renderer 重复求和逻辑删除，篡改派生金额不影响保存结果；验证：订单 renderer、service、IPC 和 preload 测试。
  - 证据：`npx vitest run src/renderer/pages/orders/index.test.tsx src/main/services/v2-order-service.test.ts src/main/ipc src/preload` 退出码 0；删除 `summarizeDraftOrderAmount` 与 `OrderDraftAmountSummary`，草稿预览改为 `createOrderAmountCalculation`（行金额委托 `calculateOrderItemAmounts`，预览直接显示公式主干）；新增服务端固化用例「忽略 renderer 附带的派生金额，保存边界只按原始字段重新计算」。
- [x] 1.7 完成阶段 A 验收，运行共享计算、订单金额、设置页和金额组件测试以及 `pnpm run typecheck`、`pnpm run lint`、`pnpm run format:check`、`git diff --check`。完成条件：阶段 A 全部验证通过并在本文件记录命令、退出码和结果后才可进入阶段 B。
  - 证据（2026-09-14）：`npx vitest run src/shared src/main/domain src/main/services/v2-order-service.test.ts src/main/ipc src/preload src/renderer/components/ui/calculated-amount src/renderer/pages/settings src/renderer/pages/reference-pages.test.tsx src/renderer/pages/orders/index.test.tsx src/renderer/styles` 退出码 0（37 文件 177 测试通过）；`pnpm run typecheck` 退出码 0；`pnpm run lint` 退出码 0（0 错误 0 警告）；`pnpm run format:check` 通过；`git diff --check` 干净。阶段 A 完成，进入阶段 B。

## 2. 阶段 B：商品资料、预计盈利与全页工作区

- [x] 2.1 修改 `src/shared/contracts/products.ts`、`src/shared/contracts/settings.ts`、`src/shared/contracts/orders.ts` 中的订单商品快照契约和 `src/main/database/v2-migrations.ts` 的最新目标结构：增加固定成本、缝边耗材、缝边提成、四道工序预计分钟及三道计时工序预计基准时薪，删除新模型的胶水用量、损耗率和旧人工成本字段。完成条件：空数据库直接建立新结构且不包含旧数据转换代码；验证：contracts 类型测试和 `src/main/database/v2-storage.test.ts` 的空库/重复启动场景。
  - 证据：迁移重编号为 12 个目标迁移（删除发货回填、订单补列、内部缝边列、损耗率列、捏毛提成补列等旧结构迁移）；空库直接建立含 `edge_consumable_cost_cents`/`fixed_cost_cents`/`expected_*_minutes`/`edge_sewing_commission_cents` 的目标结构且不含 `material_cost_cents`/`making_glue_cost_cents`/`glue_weight_milligrams`/`material_loss_rate_basis_points`/`internal_edge_cost_cents`；`runV2Migrations` 新增旧库不兼容守卫并在旧库上抛「请先备份旧业务库，再按最新结构重建数据库」；`npx vitest run src/main/database/v2-storage.test.ts` 退出码 0（8 测试：空库、V1 守卫、重复启动、旧库拒绝、履约/结算/财务约束）。
- [x] 2.2 修改 `src/main/repositories/v2-order-repository.ts`、`src/main/services/v2-order-service.ts` 和设置服务，完成新商品字段读写、非负精确输入校验及新订单商品快照冻结。完成条件：修改当前商品或设置不改写已创建的新模型订单快照；验证：repository、`v2-order-service.test.ts` 和设置服务测试。
  - 证据：`npx vitest run src/main/services/v2-order-service.test.ts src/main/services/studio-settings-service.test.ts` 退出码 0；用例「保存商品材料、成本与预计时长，并冻结到订单商品快照」验证更新商品后既有订单快照保持 `unitWeightMilligrams`/`fixedCostCents`/`expectedPackingMinutes`/`dailyCapacity` 冻结值；设置服务用例覆盖三道预计基准时薪维护与负数拒绝；全部旧字段（胶水用量/损耗率/人工成本）退出新契约、新表单与新公式。
- [x] 2.3 新增 `src/shared/calculations/product-profit.ts`，实现材料成本、捏毛装袋/缝边/打包发货预计计时人工、基础预计单件成本、预计利润、预计利润率和缝边预计增加成本；制作预计分钟不得进入计时人工。完成条件：售价为零返回不可计算利润率，订单级缝边收入不进入商品默认利润；验证：`product-profit.test.ts` 和 `product-costing.test.ts`。
  - 证据：`npx vitest run src/shared/calculations/product-profit.test.ts src/main/domain/product-costing.test.ts` 退出码 0（8 + 5 测试）；覆盖成本主线公式、缝边成本不影响默认利润、重量/时长变化同步、售价为零利润率为「—」、负利润率、负数拒绝；`calculateProductSnapshotCostCents` 改为按冻结材料单价与单件材料重量计算并纳入固定成本，不再读取旧胶水/损耗字段。
- [x] 2.4 调整 `src/shared/contracts/v2-api.ts`、`src/main/ipc/register-v2-ipc.ts` 和 `src/preload/index.ts`，让商品详情可取得主进程权威预计盈利，同时允许编辑草稿在 renderer 调用 shared 公式预览。完成条件：IPC 只接收原始字段，响应计算结果符合共享类型；验证：IPC 与 preload 契约测试。
  - 证据：新增 `v2:products:expected-profit` 通道与 `products.getExpectedProfit(productId)`，返回 `V2ProductExpectedProfit`（共享 `ProductProfitCalculation` 类型）；服务用例「读取商品权威预计盈利」验证按当前商品参数与全局预计基准时薪计算且未知商品返回 null；`npx vitest run src/main/ipc/register-v2-ipc.test.ts src/preload/v2-api.test.ts src/main/services/v2-order-service.test.ts` 退出码 0。
- [x] 2.5 重构 `src/renderer/pages/products/index.tsx`、`src/renderer/composables/use-products.ts` 和 `src/renderer/pages/app.tsx` 的商品导航状态，将新建、详情、编辑从 `YumiSheet` 迁到主内容区，并提供商品概览、成本与预计盈利、制作产能、商品存量四个标签及返回列表行为。完成条件：完整商品表单不再由抽屉承载，短确认操作仍可使用对话框；验证：商品参考页和应用导航测试。
  - 证据：商品页改为 `list/create/edit/detail` 主内容区工作区（`yumi-product-workspace`），详情含「商品概览 / 成本与预计盈利 / 制作产能 / 商品存量」四标签并提供「返回商品列表」；删除全部 `YumiSheet`（源码守卫断言 `productPageSource).not.toContain('YumiSheet')`）；`useProducts` 暴露 `getExpectedProfit`；`npx vitest run src/renderer/pages/reference-pages.test.tsx src/renderer/pages/app-components.test.ts` 退出码 0。
- [x] 2.6 在商品工作区接入 `YumiCalculatedAmount`，展示默认售价、预计单件成本、预计单件利润、预计利润率和缝边预计增加成本；任一原始字段或预计基准时薪变化时实时重算，并保持现有 YUMI 连续指标与紧凑分区风格。完成条件：公式与代入值直接可见且负利润、零售价、缺失值均有明确状态；验证：商品 renderer 交互、可访问性和样式测试。
  - 证据：编辑工作区右侧「预计盈利预览」实时调用 `calculateProductProfit`，展示默认售价、预计单件成本（含组成明细）、预计单件利润（正负色调与符号）、预计利润率（售价为零显示「—」）与缝边预计增加成本；详情标签通过 `getExpectedProfit` 读取主进程权威结果；设置页新增材料克单价与三道预计基准时薪维护；`npx vitest run src/renderer src/renderer/styles` 退出码 0。
- [x] 2.7 完成阶段 B 验收，运行商品计算、数据库、服务、IPC/preload、商品页面测试以及 `pnpm run typecheck`、`pnpm run lint`、`pnpm run format:check`、`git diff --check`。完成条件：新空库可完成商品创建、详情、编辑和新订单快照，验证证据记录后才可进入阶段 C。
  - 证据（2026-09-14）：`npx vitest run` 退出码 0（100 文件 416 测试通过）；`pnpm run lint` 退出码 0；`pnpm run format:check` 通过；`pnpm run build` 退出码 0；`git diff --check` 干净；新增 `tsconfig.check.json`（临时全量类型核查配置，最终验收前移除）确认本次改动文件无新增类型错误。阶段 B 完成，进入阶段 C。

## 3. 阶段 C：商品阶段存量与条件缝边履约

- [x] 3.1 新增 `src/shared/contracts/product-inventory.ts`，修改 `src/shared/contracts/fulfillment.ts`、`src/shared/contracts/workbench.ts` 和数据库目标结构，定义已制作待捏毛装袋、已捏毛装袋未缝边、已缝边待打包发货、已打包待发货四类物理阶段、存量事件来源、`edge_sewing` 工序及制作/捏毛装袋/缝边/打包发货目标枚举；移除新契约中的 `shipping` 岗位和 `recordOpeningWip`。完成条件：新表与约束只表达目标模型；验证：契约测试和空数据库 schema 测试。
  - 证据：`V2ProcessType` 收敛为四道目标工序、`V2FulfillmentStage` 新增 `edge_sewing`、事件类型改为目标命名并移除 `opening_wip`；新增 `product_stage_inventory_events` 目标迁移（version 13，`stage`/`quantity_delta <> 0`/来源类型约束）并在旧库守卫中加入目标列表；`npx vitest run src/main/database/v2-storage.test.ts` 退出码 0（8 测试，含空库目标结构、重复启动、旧库拒绝）。
- [x] 3.2 新增 `src/main/domain/product-inventory.ts`、`src/main/repositories/product-inventory-repository.ts` 和 `src/main/services/product-inventory-service.ts`，实现期初重录、负责人增减调整、余额汇总和投入订单事务。完成条件：余额不能为负；已捏毛未缝边存量按订单缝边需求路由，已缝边存量只满足缝边订单；投入订单同时写商品负向流水和履约正向事件，任一步失败全部回滚；验证：领域、仓储和 service 事务测试。
  - 证据：`npx vitest run src/main/domain/product-inventory.test.ts src/main/services/product-inventory-service.test.ts` 退出码 0（4 + 6 测试）：期初/调整流水可追溯、余额不能为负、无缝边订单进入待打包发货、缝边订单按需求拆分到待缝边与待打包发货、超出余额整体失败且订单履约不变、已缝边存量只满足缝边订单、商品与订单商品不一致拒绝；投入订单在单一事务内同时写入商品负向流水与履约事件，并从待制作数量扣减。
- [x] 3.3 新增商品存量 IPC/preload 契约及 `src/renderer/composables/use-product-inventory.ts`，在商品详情“商品存量”标签实现阶段汇总、期初重录、负责人调整、流水查看和投入订单。完成条件：历史现场数量不必先绑定订单，所有修改均可追溯；验证：IPC/preload 和商品存量 renderer 测试。
  - 证据：新增 5 条 `v2:product-inventory:*` 通道、preload `productInventory` API、`useProductInventory` composable 与 `ProductInventoryPanel`（阶段余额、录入历史存量、负责人调整、投入订单、最近流水）；`npx vitest run src/renderer/components/product src/main/ipc src/preload` 退出码 0（3 + 4 测试：阶段余额与流水展示、历史存量提交、超量减少保留服务端错误、投入订单只列同商品订单商品），IPC 测试断言通道与委托、preload 测试断言新通道且不含期初在制品通道。
- [x] 3.4 修改 `src/main/domain/fulfillment.ts` 和 `src/main/services/fulfillment-service.ts`，实现捏毛装袋后按 `edgeQuantity` 剩余需求分流、缝边完成进入待打包发货、打包发货完成进入已打包待发货、发货批次进入已发货。完成条件：无缝边、全量缝边、部分缝边、超量和分批发货场景均符合数量守恒；验证：fulfillment domain/service 测试。
  - 证据：`npx vitest run src/main/domain/fulfillment.test.ts src/main/services/fulfillment-service.test.ts` 退出码 0（8 + 7 测试）：捏毛完成按剩余缝边需求分流（40/60 拆分、后续不再分流）、缝边完成进入待打包发货、打包完成进入已打包待发货、分批发货受待发货数量约束；`edgeSewingRouted` 累计保留已分流数量用于剩余需求计算。
- [x] 3.5 修改履约仓储、排班任务创建、人员任务反查和 renderer 工序选择，使一条工作安排只能包含同一道目标工序任务，未选择缝边或无剩余缝边需求的订单商品不能被安排缝边；排产进度、超派提示和待派数量按目标工序的履约阶段计算，只有制作显示合格/不合格，界面统一把 `packing` 显示为“打包发货”。完成条件：新 UI 不再出现独立 `shipping` 岗位、非制作质检状态或订单绑定期初在制品入口；验证：履约、排班、人员详情和工作台 renderer 测试。
  - 证据：`use-fulfillment` 队列阶段收敛为制作/捏毛装袋/缝边/打包发货（`ready_to_ship` 退出可派工阶段），排班表单不再要求计划分钟、不再为 `shipping` 提供“不关联订单产品”分支；履约页删除期初在制品整块入口与页头按钮、阶段总量补齐缝边；工作安排页工序标签含缝边、质检表单仅制作任务渲染、文案改为“质量确认”；订单页履约阶段补齐缝边与“打包发货”；`npx vitest run src/renderer` 退出码 0（57 文件 243 测试）。
- [x] 3.6 修改完成结果与质量接口，使只有制作接受完成、合格和不合格数量，且合格加不合格等于完成；捏毛装袋、缝边和打包发货只接受完成数量。完成条件：非制作质量字段在契约、service 和页面三层均被拒绝；验证：domain、service、IPC 和 renderer 测试。
  - 证据：main 服务仅接受制作任务质量确认（其他工序抛「只有制作任务需要质量确认」），非制作工序提交完成结果即按完成数量流转；页面层质量确认表单只在 `task.processType === 'making'` 时渲染；`npx vitest run src/main/domain src/main/services src/main/ipc src/renderer/pages/work-assignments src/renderer/pages/fulfillment` 退出码 0。
- [x] 3.7 完成阶段 C 验收，运行商品存量、履约、排班、发货、数据库、IPC/preload 和 renderer 测试以及 `pnpm run typecheck`、`pnpm run lint`、`pnpm run format:check`、`git diff --check`。完成条件：新订单可从制作流转到条件缝边、打包发货和发货，存量投入订单保持原子性，证据记录后才可进入阶段 D。
  - 证据（2026-09-14）：`npx vitest run` 退出码 0（103 文件 431 测试通过，含四工序条件流转、缝边需求守卫、存量投入原子性、分批发货数量守恒）；`pnpm run lint` 退出码 0；`pnpm run format:check` 通过；`pnpm run build` 退出码 0；`git diff --check` 干净；临时 `tsconfig.check.json` 确认本阶段改动文件无新增类型错误。阶段 C 完成，进入阶段 D。

## 4. 阶段 D：负责人次日工时核算与工资重构

- [x] 4.1 新增 `src/shared/contracts/work-time-reviews.ts` 和数据库目标表，定义员工、日期、单一计时工序、核算分钟、个人时薪快照、来源、外部记录字段、草稿/已确认/已作废状态、工作安排关联及多商品结果关联。完成条件：制作不能创建工时记录，同一工作安排最多进入一条未作废核算，当前 UI 来源固定为 `manual_review`，不提供 SD 卡输入；验证：契约和数据库约束测试。
  - 证据：新增 `work_time_reviews` / `work_time_review_assignments` / `work_time_review_items` 目标表（version 14，工序仅捏毛装袋/缝边/打包发货、`approved_minutes > 0`、已确认必须有冻结时薪、来源限定手工核算与未来设备、明细唯一任务）并纳入旧库守卫；`npx vitest run src/main/database/v2-storage.test.ts` 退出码 0（8 测试）。
- [x] 4.2 新增 `src/main/domain/work-time-review.ts`、`src/main/repositories/work-time-review-repository.ts` 和 `src/main/services/work-time-review-service.ts`，实现草稿、确认、受约束作废、时薪冻结以及同一事务提交多商品完成结果。完成条件：一条记录禁止跨工序，确认后禁止直接修改，重复确认不重复写结果；仅在结果未被下游消耗且工时未进入已确认结算时可原子作废，否则拒绝直接作废，并通过后续结算中关联原工时与原结算的正负调整保留审计关系；验证：domain、repository 和 service 状态/事务测试。
  - 证据：`npx vitest run src/main/services/work-time-review-service.test.ts` 退出码 0（4 测试）：制作被拒绝、一段 180 分钟工时登记两个商品明细并在确认事务内提交完成结果、确认冻结 3000 分个人时薪、重复确认幂等且不重复增加数量、已确认不可直接修改、同一工作安排重复核算被拒绝、完成明细必须来自所关联工作安排、未被消耗时可原子作废并回退任务为待完成后重新核算、被下游打包消耗后拒绝作废。
- [x] 4.3 新增工时核算 IPC/preload 契约与 `src/renderer/composables/use-work-time-reviews.ts`，将排班页面明确拆成“安排”和“待核算”：制作走结果质量确认，三道计时工序选择一个或多个员工、日期和工序一致的工作安排后，录入整段时长及多商品完成明细。完成条件：排班时不要求最终时长和最终数量，同一工作安排不能重复核算，负责人可在第二天完成两类确认；验证：排班/履约 renderer、IPC 和 preload 测试。
  - 证据：6 条 `v2:work-time-reviews:*` 通道、preload `workTimeReviews` API 与 runtime 装配；`useWorkTimeReviews` composable（草稿/确认/作废、按员工与日期装载候选并按商品快照带出该工序预计单件分钟、排除已核算安排）；排班页新增「待核算」视图，上半区为「制作结果待确认」（进入结果与质量确认），下半区为「计时工序待核算」+「已登记工时核算」；`npx vitest run src/renderer/pages/fulfillment src/renderer/components/fulfillment src/main/ipc src/preload` 退出码 0（含跨工序限制、候选排除、保存并确认调用链、作废对话框）。
- [x] 4.4 在共享计算层增加预计总分钟、时间差和预计效率结果，并在待核算页面展示负责人核算时长、预计时长、差异、效率和商品明细。完成条件：实际高于预计只提示复核，不自动扣薪或阻止确认；验证：公式测试和待核算页面交互测试。
  - 证据：新增 `src/shared/calculations/work-time.ts`（预计总分钟 = Σ 完成数量 × 预计单件分钟、时间差、预计效率万分比；核算分钟为零时效率不可计算），`npx vitest run src/shared/calculations/work-time.test.ts` 退出码 0（4 测试）；待核算面板实时展示预计总分钟、时间差、预计效率与商品明细，实际高于预计仅显示「实际用时高于预计，请核对」且不阻止确认（交互测试断言 40×3+30×2=180 / 核算 240 → 时间差 60、效率 75.00%，仍可保存并确认）。
- [x] 4.5 修改 `src/main/domain/settlement.ts`、`src/main/services/settlement-service.ts`、结算仓储和 `src/shared/contracts/settlements.ts`：制作按合格提成减不合格材料成本且无时薪，捏毛装袋/缝边按确认工时加冻结提成，打包发货仅按确认工时；工资期间按工作日期而非确认日期归属；移除周期级 `attendanceMinutes`、排班分钟双口径及两套参考结果。完成条件：结算只保存一套可追溯计算候选来源汇总和负责人最终实发；一条工时最多进入一个有效结算；已结算制作质量更正只形成材料成本待退款；已结算工时差异可在后续草稿结算中建立关联原工时与原结算的正负调整；现有抵扣、顺延和财务流水事务仍成立；验证：settlement domain/service/repository 测试。
  - 证据：契约改为 `timedWageCents` / `commissionCents` / `materialDeductionCents` / `adjustmentCents` / `candidateWageCents` 与制作来源、计时来源（含多商品明细）、来源关联调整、材料扣款与材料待退款；数据库目标结构重建 `worker_settlements`、`worker_settlement_making_sources`、`worker_settlement_timed_sources`（部分唯一索引保证一条已确认工时最多进入一个有效结算）、`worker_settlement_timed_items`、`worker_settlement_adjustments`，扣款与退款表只保留材料列；制作按合格数量计件并以冻结材料克单价 × 单件材料重量扣不合格成本且无时薪，捏毛装袋/缝边按确认工时加完成提成，打包发货只按确认工时；工资期间按工作日期归属且草稿工时不入结算；`npx vitest run src/main/domain/settlement.test.ts src/main/services/settlement-service.test.ts src/main/services/work-time-review-service.test.ts src/main/application` 退出码 0（6 + 6 + 4 + E2E：归属期、重复纳入拒绝、已结算制作更正只形成材料待退款、已结算工时 -1500 分来源调整、抵扣顺序与顺延、确认财务流水与重复确认拒绝）；`npx vitest run` 退出码 0（106 文件 444 测试）。
- [x] 4.6 修改 `src/renderer/pages/settlements/index.tsx` 和 `src/renderer/composables/use-settlements.ts`，按工序展示核算分钟、时薪快照、计时工资、商品完成数量、计件提成和制作材料扣款，并提供已确认来源只读查看；工时更正入口必须遵守下游履约与确认结算约束。完成条件：页面不再要求负责人填写整周期模糊考勤分钟，也不允许静默作废已进入确认结算的工时；验证：结算 renderer 测试和可访问性测试。
  - 证据：结算详情改为按口径展示「计时工资 / 计件提成 / 材料扣款与来源调整 / 计算候选应发 / 本期抵扣与顺延」，并列出制作结果来源（合格/不合格、冻结提成、提成、材料扣款）、计时来源（工序、工作日期、核算分钟、冻结时薪、计时工资、完成数量提成与商品明细）、来源关联调整与扣款来源；删除整周期考勤分钟与考勤备注输入，人员字段只保留本期实际扣款、其他调整、最终实发、付款日期与负责人备注；新增 `addWorkTimeAdjustment` 通道（契约/preload/IPC/composable）并在草稿结算中提供「已结算工时更正」表单，选择已确认工时并录入更正分钟与原因，服务端仍强制「必须已进入确认结算」且重复调整被拒绝；`npx vitest run src/renderer/components/settlement src/renderer/pages/settlements src/renderer/pages/app-components.test.ts` 退出码 0（详情 5 测试 + 页面 8 测试，覆盖无考勤输入、只读已确认结算、来源表内容与调整提交），源码守卫断言不再出现「排班口径 / 考勤口径 / 考勤总分钟」。
- [x] 4.7 修改 `src/main/domain/product-costing.ts`、订单成本详情 service 与页面，停止把多商品计时工资分摊成订单历史实际成本；展示订单商品快照预计成本及订单缝边预计增量利润，并明确预计口径。完成条件：不存在按数量或预计分钟伪造实际人工/实际利润的路径；验证：`order-cost-details` 相关 domain、service 和 renderer 测试。
  - 证据：`product-costing.ts` 只按订单商品快照计算预计直接成本（材料、包装、配饰、替换袋、固定成本与缝边耗材），无任何按数量或预计分钟分摊计时工资的路径；订单成本详情新增 `expectedEdgeIncrementalCostCents`（每件缝边预计增加成本 = 缝边耗材 + 缝边提成 + 预计缝边计时人工）与 `expectedEdgeIncrementalProfitCents`（缝边对客单价 − 每件预计增加成本），ReportService 注入工作室预计基准时薪；页面「已知成本构成」改为「预计成本构成」（人工及其他 → 实际计时人工「不分摊」），商品表新增「缝边预计增量利润 / 件」列，核算边界说明改为「不把实际计时工资按数量或预计分钟分摊为订单实际成本」；删除已无调用方的旧模型死代码 `src/main/domain/production.ts`（实际分钟 × 时薪伪造成本）；`npx vitest run src/main/services/report-service.test.ts src/renderer/pages/orders` 退出码 0（新增用例：缝边单价 3 元 − 预计增加成本 1.2 元 = 每件增量利润 ¥1.80；订单页断言预计口径与占位符）；`npx vitest run` 退出码 0（105 文件 441 测试）。
- [x] 4.8 补齐从订单创建、四工序排班、制作质量确认、次日多商品工时核算、条件缝边、打包发货到工资确认和财务流水的 E2E；覆盖同一时段处理两个商品、跨周次日确认仍按工作日期归属、制作不合格、实际时长高于预计、同一工作安排重复核算、重复确认、重复结算、未被引用工时作废重录、已被下游或确认结算引用时拒绝直接作废，以及已结算工时差异进入后续来源关联调整。完成条件：业务事实数量守恒且工资公式与展示公式一致；验证：`src/main/application/v2-order-workflow.e2e.test.ts` 及新增工时工资 E2E。
  - 证据：新增 `src/main/application/v2-work-time-payroll.e2e.test.ts`（2 个用例，经 `V2ApplicationRuntime` 全链路）：用例一覆盖订单（两商品，其一选缝边 4 件）→ 制作（9 合格 1 不合格 / 6 合格）→ 一段 240 分钟捏毛核算登记两商品并按剩余缝边需求分流 4/5 → 缝边 4 件（60 分钟）→ 打包发货（120 分钟，实际高于预计 45 分钟仅提示核对）→ 分批发货数量守恒 → 周结算的计时工资 21,000 分、计件提成 5,935 分（制作 4,500 + 捏毛 1,275 + 缝边 160，打包不产生提成）、材料扣款 9 分，逐项与共享公式重算结果一致，确认后写入财务流水并反映到月度经营报表；用例二覆盖同一工作安排重复核算拒绝、重复确认幂等、未被消耗工时原子作废并重录、跨周按工作日期归属（09-13 工时归入 09-07～09-13）、已进入确认结算的工时拒绝作废、重复结算拒绝、发货消耗后拒绝作废、已结算工时 −750 分来源关联调整与重复调整拒绝。E2E 同时发现并修复真实缺口：缝边任务未冻结商品缝边提成（`fulfillment-service` 现按 `edgeSewingCommissionCents` 冻结）。`npx vitest run src/main/application` 退出码 0（含既有 `v2-order-workflow.e2e.test.ts`）；`npx vitest run` 退出码 0（106 文件 443 测试）。
- [x] 4.9 编写新模型部署与回滚说明：备份旧数据库、重建空目标库、重新录入商品及现场商品阶段存量、失败时恢复旧应用和旧库；不得包含旧业务数据导入步骤。完成条件：部署说明清楚区分“备份保留”与“不迁移旧数据”；验证：人工文档复核和 `git diff --check`。
  - 证据：新增 `docs/2026-09-14-v2-target-model-deployment-and-rollback.md`，包含：结论对照表（备份保留=必须 / 旧数据导入=禁止 / 重建库=必须 / 重新录入=必须）；新旧模型差异摘要；部署步骤（关闭实例 → 备份 `yumi-studio-v2.sqlite` 与 `attachments-v2/` 并验证副本可读 → 删除旧库含 `-wal`/`-shm` → 启动建立空库，误留旧库时应用按兼容守卫拒绝并提示备份后重建）；重新录入手册（设置页材料克单价与三道预计基准时薪、人员时薪、商品全页工作区、商品存量四阶段、投入订单规则）；发布后验证清单；回滚步骤（成对替换应用版本 + 数据库文件，明确新旧库不得共用）；常见问题明确"不提供导入路径"。文中如实引用 `v2-storage.ts` 的文件名常量与守卫提示文案；`npx prettier --check` 与 `git diff --check` 通过。
- [x] 4.10 完成全提案验收，运行 `pnpm test`、`pnpm run typecheck`、`pnpm run lint`、`pnpm run format:check`、`pnpm run build`、`git diff --check` 和 `openspec validate redesign-product-process-payroll-and-costing --strict`，读取完整输出并记录证据。完成条件：同一提案四阶段全部完成且所有新鲜验证通过后，才可将提案状态改为已完成；归档仍需另行授权。
  - 证据（2026-09-14，全部新鲜运行）：`pnpm test` 退出码 0（106 文件 443 测试通过，含 `pnpm test` 的 pretest 重新构建 better-sqlite3 成功）；`pnpm run typecheck` 退出码 0；`pnpm run lint` 退出码 0（0 错误 0 警告）；`pnpm run format:check` 通过；`pnpm run build` 退出码 0；`git diff --check` 干净；`openspec validate redesign-product-process-payroll-and-costing --strict` 输出「Change is valid」退出码 0。四阶段任务全部完成；临时类型核查配置 `tsconfig.check.json` 已删除。提案状态改为「已完成实施，待授权归档」；归档仍需另行授权。
